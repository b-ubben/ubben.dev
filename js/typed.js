import Typed from 'typed.js';

const options = {
  autoInsertCss: true,
  loop: true,
  showCursor: false,
  shuffle: true,
  smartBackspace: true,
  startDelay: 0,
  stringsElement: '.header__figure__caption__list',
  typeSpeed: 69,
};

const typed = new Typed('.header__figure__caption', options);
